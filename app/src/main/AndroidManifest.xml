<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.hackathon2019summer">

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".TaxRateImageActivity"></activity>
        <activity android:name=".EatOutActivity" />
        <activity android:name=".AlcoholActivity" />
        <activity android:name=".WaterActivity" />
        <activity android:name=".BeverageActivity" />
        <activity android:name=".MealActivity" />
        <activity android:name=".NewsPaperActivity" />
        <activity android:name=".ResultActivity" />
        <activity android:name=".CheckTaxRateMasterActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>